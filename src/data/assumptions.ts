/**
 * @deprecated Import from `../brain` instead.
 * Re-exports kept so proposer adapters stay thin.
 */
export type { Assumption, AssumptionStatus } from '../brain'
export { ASSUMPTION_CLASS, demoAssumptions as initialAssumptions } from '../brain'
